# V0.1 validation and release

Working directory: E:\EastQuest. The clean original V0 commit
753e0c41d5b06cd0681aa9b97f1866bfbf58967f was preserved. Local main was established
at that commit and both main and feat/v0-campus-exploration were pushed.
All new development uses feat/v0.1-playtest-foundation.

## Implementation checks

- 34 tests pass: original 24 plus 10 playtest/content checks.
- Lint, TypeScript and production build passed.
- No GPS, QR, camera, ML dependencies or multiplayer were added.
- Campus data, QuestEngine, mini-game registry and persistence behavior remain intact.
- CI runs npm ci, lint, typecheck, test and build on main pushes and PRs into main.
- Node.js 22 LTS matches the locally validated 22.21.0 runtime.
  See [Node release status](https://nodejs.org/en/about/previous-releases)
  and [setup-node caching](https://github.com/actions/setup-node).

## Browser evidence

Headless Chromium 152 through agent-browser and its CDP connection.
Moved the actual Phaser player; no teleportation or save edits.

- Login with the sample ID, Main Gate tutorial, Explorer Pass and 50 XP.
- WASD movement, E interaction, paused movement during quests, wrong-answer retry.
- Walked all five locations; collision and world bounds hold; 450 XP / five keys.
- Collection and profile retain progress across page reloads.
- All five routes at 390, 430 and 768 pixels: no horizontal overflow.
- D-pad and interaction card do not overlap at those widths.
- Emulated touch moves the player and release stops it.
- Phone quest dialog fits; fullscreen enters on the game only.
- Normal game hides debug; ?debug=1 exposes position, interaction, quests, keys,
  version and LOCAL mode. Diagnostics sample at 4 Hz and can collapse.
- Manifest and both icons load.
- No runtime exceptions in the completed gameplay and layout checks.
- Feedback EXPORT action saves a real JSON file with rating/version/timestamp and
  notes, omitting student ID and credentials. The small, bounded export uses a
  self-contained data URL. Browser automation uses a normalized Windows download path.

Screenshots and repeatable browser scripts are kept in ignored .artifacts.
Emulation is not a physical phone test. Follow playtesting.md on actual Android/iOS.

## Deployment boundary

Hosting configuration is separate from game logic: the same Next.js build runs
locally and on Vercel. No repository secrets or backend setup are needed for local
mode. Preview and production use one dedicated taoshifs-projects/eastquest project.
The Git integration deploys feature pushes as previews and main as production.
Do not create a second EastQuest project or deploy into a portfolio project.

Progress is browser- and origin-specific. Preview, production and localhost each
have separate local saves. Do not mistake that for lost server progress.
Keep the student-built / unofficial disclaimer and closed-playtest labeling.

## GitHub tooling

The gh command is not installed. Existing Git credentials authorize pushes and
GitHub REST operations; the connected GitHub app is read-only. Repository metadata
was configured through GitHub REST without saving or printing credentials.

For a teammate who wants the CLI on Windows:
```powershell
winget install --id GitHub.cli -e
# Open a fresh terminal
gh auth login --hostname github.com --web
gh auth status
gh repo view Taoshif1/EastQuest
```

The repository already exists. Do not initialize another README or run git init.

## Hosted verification

Hosted URL: https://eastquest.vercel.app. Explicit preview:
https://eastquest-jd5m5izuh-taoshifs-projects.vercel.app (deployment protection retained).
Preview build passed on Node 22; authenticated HTTP check returned 200.
The public hosted build passed the full five-quest walking route, 450 XP, persistence,
all five routes at 390/430/768px, real emulated touch, quest dialog, fullscreen,
manifest/icons, debug/version/disclaimer checks with no runtime exceptions.
GitHub PR: https://github.com/Taoshif1/EastQuest/pull/1. Initial CI passed.
Final commit checks and main release results are recorded in the GitHub PR and tag.

Vercel automatically promoted its first Git-triggered deployment before merge.
An explicit preview was then created in the same project. Production branch is main,
and the project Node version is now 22.x to match CI and local validation.
Future local CLI uploads exclude test artifacts and environment files via .vercelignore.
